json.cache! space do
  json.id             space.id
  json.width          space.width
  json.length         space.length

  json.created_at     space.created_at
  json.updated_at     space.updated_at
end
